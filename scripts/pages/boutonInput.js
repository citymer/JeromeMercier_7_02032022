// BOUTON INPUT INGREDIENTS

/*function createIngredient(id) {
    
    const contentIngredients = document.querySelector('.content_ingredients');
    
    let ingredientList = document.createElement('p');
    contentIngredients.appendChild(ingredientList);
    ingredientList.id = id;
    ingredientList.textContent = id.ingredient;
}



createIngredient();

function dd() {
    ingredients.forEach((id) => {
        createIngredient(id);
    })
}
*/
